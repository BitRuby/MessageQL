
type Conversation {
    id: ID!
    users: [User]
    creator: User
}

type Query {
    getConversation(id: String): Conversation
   
}

type Mutation {
    joinConversation(id: String): Conversation
    leaveConversation(id: String): Conversation
    saveConversation: Conversation
}
